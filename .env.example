# Free key: https://developer.nlr.gov/signup/  (DEMO_KEY works for light testing)
NREL_API_KEY=

# Optional: set to 1 to log requests/cache activity to stderr
SOLAR_MCP_DEBUG=

# Optional: override the cache directory (default ~/.cache/solar-mcp)
SOLAR_MCP_CACHE_DIR=
